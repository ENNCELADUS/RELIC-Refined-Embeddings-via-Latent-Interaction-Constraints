# Agent Guidelines Alias

This file exists as a compatibility alias for `AGENTS.md`.

## Environment Requirement (Critical)
- Always run Python-related commands in this repo from the `relic` conda environment.
- Required first step: `conda activate relic`
